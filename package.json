{
  "name": "electron-test",
  "version": "1.0.0",
  "description": "electron basic usage",
  "main": "index.js",
  "scripts": {
    "build": "tsc && node esbuild.config.js",
    "build1": "tsc && node esbuild.config.js",
    "build0": "tsc && node init.js",
    "electron": "electron out/index.bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "electron"
  ],
  "author": "JC",
  "license": "MIT",
  "devDependencies": {
    "@types/electron": "^1.4.38",
    "@types/node": "^22.10.1",
    "electron": "^33.2.1",
    "esbuild": "^0.24.0",
    "nodemon": "^3.1.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "electron-test": "file:"
  }
}
